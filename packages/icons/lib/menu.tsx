import { createFontawesomeIcon } from "./+fontawesomeIcon";

import { faBars as solid } from "@fortawesome/free-solid-svg-icons";

const MenuIcon = createFontawesomeIcon({
  solid,
});

export default MenuIcon;
