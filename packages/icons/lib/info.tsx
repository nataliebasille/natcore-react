import {
  faInfo as solid,
  faInfoCircle as solidCircle,
} from "@fortawesome/free-solid-svg-icons";
import { createFontawesomeIcon } from "./+fontawesomeIcon";

const InfoIcon = createFontawesomeIcon({
  solid,
  solidCircle,
});

export default InfoIcon;
