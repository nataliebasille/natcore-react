import { createFontawesomeIcon } from "./+fontawesomeIcon";

import {
	faPercent as solid,
	faPercentage as solidOpen,
} from "@fortawesome/free-solid-svg-icons";

const PercentIcon = createFontawesomeIcon({
	solid,
	solidOpen,
});

export default PercentIcon;
