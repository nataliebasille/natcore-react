import { faStripeS as s } from "@fortawesome/free-brands-svg-icons";
import { createFontawesomeIcon } from "./+fontawesomeIcon";

const StripeIcon = createFontawesomeIcon({
	s,
});

export default StripeIcon;
