import { createFontawesomeIcon } from "./+fontawesomeIcon";
import { faPlus as solid } from "@fortawesome/free-solid-svg-icons";

const PlusIcon = createFontawesomeIcon({
	solid,
});

export default PlusIcon;
